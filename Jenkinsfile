pipeline {
    agent any
    stages {
        stage('Install dependencies') {
            steps {
                echo "installing"
              //  echo "sh 'npm install'"
            }
        }

        stage('Build') {
            steps {
                echo "building"
               // echo "sh 'npm run build'"                
            }
        }
    }
}
